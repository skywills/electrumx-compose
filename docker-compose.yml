version: '3'
services:
    electrumx:
      image: lukechilds/electrumx
      ports: 
        - "${SERVICE_PORT}:50001"
      environment:
        - 'DAEMON_URL=${DAEMON_URL}'
        - 'COIN=${COIN}'